import React from "react";
import ReactDOM from "react-dom/client";
import Hercules from "../components/HerculesMain";

ReactDOM.createRoot(document.getElementById("app")!).render(
  React.createElement(Hercules)
);